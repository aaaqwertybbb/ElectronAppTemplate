I mess about with a custom bundler "preprocessor.cjs", don't do this, I'm just seeing what happens.

I use JavaScript, don't do this, probably you want typescript, I'm just seeing what happens.

I'm thinking if I were to squeeze the editor repo into the smallest amount of code, I'm wondering what that would look like.
(smallest as in --- remove all the editor related logic)

=============

> npm install

> npm run build

> npm run start
